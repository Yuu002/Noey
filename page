Odoo Client Error

UncaughtClientError > ReferenceError
Uncaught Javascript Error > moment is not defined

Occured on localhost:10018 on 2026-01-21 10:40:59 GMT

ReferenceError: moment is not defined
    at SaleMonthlyForecastChart.openInvoiceReport (http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:23985:37)
    at Chart.onClick (http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:23984:790)
    at callback (http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:21812:325)
    at Chart._handleEvent (http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:22856:389)
    at Chart._eventHandler (http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:22854:20)
    at listener (http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:22840:235)
    at Chart.<anonymous> (http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:22289:131)
    at http://localhost:10018/web/assets/7acd91f/web.assets_web.min.js:21879:224

C:\Users\noy\Desktop\odoo_module\sales_monthly_forecast\static\src\js\sales_monthly_forecast_chart.js
/** @odoo-module **/

import {registry} from "@web/core/registry";
import {Component, onMounted, useRef} from "@odoo/owl";
import {useService} from "@web/core/utils/hooks";

export class SaleMonthlyForecastChart extends Component {
    setup() {
        this.orm = useService("orm");
        this.actionService = useService("action");
        this.chartRef = useRef("chart");

        onMounted(() => {
            this.renderChart();
        });
    }

    async renderChart() {
        const recordId = this.props.action.context.active_id;

        const result = await this.orm.call(
            "sale.monthly.forecast",
            "get_forecast_chart_data", [recordId], {}
        );

        if(!result || !this.chartRef.el) {
            return;
        }

        const ctx = this.chartRef.el.getContext("2d");

        new Chart(ctx, {
            type: "line",
            data: {
                labels: result.labels,
                datasets: [{
                        label: "Actual Sales",
                        data: result.actual,
                        borderColor: "#469fdf",
                        backgroundColor: "rgba(31,119,180,0.2)",
                        borderWidth: 2,
                        tension: 0.3,
                        fill: true,
                    },
                    {
                        label: "connection",
                        data: result.connection,
                        borderColor: "#df6666",
                        backgroundColor: "rgba(255, 126, 126, 0.23)",
                        borderWidth: 2,
                        tension: 0.3,
                        fill: true,
                        pointRadius: 0,
                    },
                    {
                        label: "Forecast",
                        data: result.forecast,
                        borderColor: "#df6666",
                        backgroundColor: "rgba(255, 110, 110, 0.23)",
                        borderDash: [5, 5],
                        borderWidth: 2,
                        tension: 0.3,
                        fill: true,
                        pointRadius: 5,
                    },
                ],
            },
            options: {
                responsive: true,

                onClick: (evt, elements) => {
                    if(!elements.length) return;

                    const element = elements[0];
                    if(element.datasetIndex !==0) return;

                    const index = element.index;
                    const label = result.labels[index];

                    this.openInvoiceReport(label);
                },
                onHover:(evt, elements) => {
                    const canvas = evt.native.target;
                    if(
                        elements.length &&
                        elements[0].datasetIndex === 0
                    ) {
                        canvas.style.cursor = "pointer";
                    } else {
                        canvas.style.cursor = "default";
                    }
                },

                plugins: {
                    legend: {
                        position: "top",
                        labels: {
                            filter: function(item) {
                                return item.text !== "connection";
                            }
                        }
                    },
                },
                scales: {
                    y: {
                        beginAtZero: true,
                    },
                },
            },
        });
    }

    openInvoiceReport(label) {
        const date = moment(label, "MMMM YYYY");

        const start = date.startOf("month").format("YYYY-MM-DD");
        const end = date.endOf("month").format("YYYY-MM-DD");

        this.actionService.doAction({
            type: "ir.actions.act_window",
            name: `Invoice Report ${label}`,
            res_model: "account.invoice.report",
            view_mode: "tree,pivot,graph",
            domain: [
                ["invoice_date", ">=", start],
                ["invoice_date", "<=", end],
            ],
        });
    }
}

SaleMonthlyForecastChart.template = "sale_monthly_forecast_chart_template";
registry.category("actions").add(
    "sale_monthly_forecast_chart",
    SaleMonthlyForecastChart
);
